# Phase 5 style tokens

`era_ph5_style_v1.0.md` · Unit 5A of `direction_ph5_tableau_v1.0.md` · generated by `era_ph5_extract.py` · **Ruling P5-4**

Austere architectural presentation: maximum negative space, structural alignment, one sans-serif, **strict monochromatic — one accent, distinction by tint, tone and shade only, on stark white**, text reduced to labels. `era_ph5_workbook_gate.py` (Unit 5B) asserts that the colours in the saved `.twb` are drawn from this list and nothing else.

## Colour — one hue, 210°

Every token below is generated from HSL hue **210°**. Gate `P6c` recomputes the hue back out of each hex and requires them all to agree, so a token cannot be off-hue and still be named on-hue.

| token | hex | S | L | use |
| --- | --- | --: | --: | --- |
| `--era-surface` | `#FFFFFF` | 0.00 | 1.000 | page and sheet background: stark white |
| `--era-surface-alt` | `#EFF4F8` | 0.40 | 0.955 | banded row / panel fill, barely off white |
| `--era-grid` | `#E7EDF3` | 0.35 | 0.930 | gridlines, where a gridline is unavoidable |
| `--era-rule` | `#CCD7E3` | 0.30 | 0.845 | hairline rules and axis lines |
| `--era-muted` | `#8799AB` | 0.18 | 0.600 | secondary labels, footnotes, EXCLUDED and UNPRICED marks |
| `--era-accent-100` | `#D0E0F0` | 0.52 | 0.880 | lightest ramp step: smallest magnitude |
| `--era-accent-200` | `#A2C2E2` | 0.52 | 0.760 | ramp step 2 |
| `--era-accent-300` | `#73A3D3` | 0.52 | 0.640 | ramp step 3 |
| `--era-accent-400` | `#4585C4` | 0.52 | 0.520 | ramp step 4: the primary accent |
| `--era-accent-500` | `#336BA3` | 0.52 | 0.420 | ramp step 5 |
| `--era-accent-600` | `#27527C` | 0.52 | 0.320 | ramp step 6 |
| `--era-accent-700` | `#1B3855` | 0.52 | 0.220 | darkest ramp step: largest magnitude |
| `--era-ink` | `#111A22` | 0.35 | 0.100 | primary type |

**Rules.**

1. **No second hue, anywhere.** A diverging measure (saving against cost) uses the two ends of the accent ramp — `--era-accent-100` to `--era-accent-700` — never two hues. Saving and cost are one quantity with a sign.
2. **Sequential measures** use the ramp in order; **categorical** distinctions use position and label, not colour. If a view needs more than seven colour steps it needs a different view.
3. **Excluded and unpriced quantities are drawn in `--era-muted` with a hairline outline**, never filled in accent — they are present and they are not in the total.
4. **`--era-surface` is the only background.** No panel fills except `--era-surface-alt`; no shadows; no borders except `--era-rule`.

## Type

One family: `Helvetica Neue, Helvetica, Arial, sans-serif`. Five sizes and no others.

| role | px |
| --- | --: |
| caption | 11 |
| body | 13 |
| label | 16 |
| subhead | 22 |
| title | 34 |

## Grid

- Base unit **8 px**; every dimension is a multiple of it.
- Outer margin **48 px**; gutter between panes **24 px**.
- Axis rules `--era-rule` at 1 px. **Gridlines off by default**; where a value must be read off an axis, `--era-grid` at 1 px and nothing heavier.
- Titles left-aligned to the content edge. No centred text.

## Text

Text is reduced to labels. The images and the graphics carry the argument; explanation belongs in the caveats sheet (G4) and in the report, not on the marks. **Ruling P5-3's caveats sheet is DATA, and Unit 5C's hard requirement is zero typed figures anywhere in the workbook** — every number on screen resolves to a field.

